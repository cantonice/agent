---

name: plan-feature
description: 在开发新功能之前，通过阅读现有代码并与用户交互，明确需求、技术路线、改动范围和验证方式。适用于在已有项目上开发新 feature，但尚未完全确定实现方案的场景。
---------------------------------------------------------------------------------------------

在编写或修改任何代码之前，先与用户一起确定这个 feature 应该如何实现。

目标是基于两方面信息形成清晰的技术方案：

1. 用户真正想解决的问题
2. 项目当前已有的实现

在技术路线达成一致之前，不要开始实现。

## 核心原则

**先探索，再提问。**

如果一个问题可以通过阅读 repository 中的代码、配置、测试或文档得到答案，就自己调查，不要询问用户。

只有涉及以下情况时才询问用户：

* 产品需求和预期行为
* 无法从代码中确定的信息
* 存在多种合理技术方案
* 需要做工程 trade-off
* 可能明显影响现有 architecture 的决策

## 工作流程

### 1. 理解需求

首先明确：

* 这个 feature 要解决什么问题？
* 希望增加或改变什么行为？
* 哪些现有行为应该保持不变？
* 什么情况下算开发成功？

对于关键但不明确的需求，不要自行假设。

### 2. 调查现有实现

在提出技术方案之前，先调查代码库：

* 找到与需求相关的 module
* 理清当前 call path 和 data flow
* 搜索是否已经存在类似功能
* 找到可以复用的 abstraction、interface 和 helper
* 查看相关 configuration
* 查看相关 tests

先理解这个 feature 应该如何融入现有系统。

### 3. 找出需要决策的问题

根据现有代码，识别真正需要决定的技术问题。

例如：

* 新逻辑应该放在哪里？
* 应该扩展已有 abstraction，还是增加新的 abstraction？
* 是否需要增加新的 interface？
* 是否需要增加 configuration？
* 是否会影响现有 behavior？
* 是否需要考虑 backward compatibility？
* 不同实现方案有什么 trade-off？

如果答案已经可以从现有代码中确定，就不要询问用户。

### 4. 与用户逐个确定技术决策

一次只讨论一个重要问题。

每个问题尽量包含：

1. 为什么需要做这个决定
2. 当前代码是什么情况
3. 有哪些现实可行的方案
4. 各方案主要 trade-off
5. 推荐哪个方案，以及为什么

然后等待用户决定，再继续下一个问题。

如果后面的决策依赖前面的决策，按照依赖关系依次讨论。

不要一次向用户抛出大量问题。

### 5. 输出技术方案

当重要问题都确定后，整理最终方案。

至少包括：

#### 目标

这个 feature 最终要实现什么。

#### 当前实现

相关模块目前是如何工作的。

#### 技术路线

最终确定采用什么方式实现，以及为什么。

#### 预计改动

预计修改哪些 module / file / component，以及各自承担什么职责。

#### 接口变化

是否涉及：

* API
* configuration
* data structure
* input/output
* public interface

如果没有变化，也明确说明。

#### 兼容性

哪些现有 behavior 必须保持不变。

#### 验证方式

如何确认：

* 新 feature 正常工作
* 原有功能没有发生 regression

#### 未解决问题

记录有意暂时不解决的问题，避免在实现阶段偷偷做出新的设计决策。

## 开始开发

技术方案总结完成后停止。

除非用户明确要求开始实现，否则不要直接修改代码。
